# H-RAMOS 核心机制原理说明

> 基于代码实际运行逻辑（`ababc_utils.py` 为03/04/05/06的统一共享模块），非论文声称

---

## 一、提示词迭代原理（04_提示词自动优化流水线）

### 1.1 它不是什么

**不是模型在学习。** Meta-LLM每次API调用都是独立的对话——它不记得上一轮做了什么。提示词优化不是gradient descent，不是fine-tuning，不是reinforcement learning。

### 1.2 它实际是什么

**自动化错误分析 + 定向提示词改写。** 每一轮的工作流程：

```
┌─────────────────────────────────────────────────────────┐
│ Round N 的执行流程                                       │
├─────────────────────────────────────────────────────────┤
│ 1. 用 current_prompts 在100条测试集上跑完整ABABC          │
│    (调用 ababc_utils.run_ababc_pipeline 或等价本地实现)   │
│ 2. 计算指标: Kappa, F1, Precision, Recall, Accuracy      │ 
│ 3. 分析瓶颈: B1拒绝率, A2实际修改率, C使用频率             │
│ 4. 构建趋势报告: 连续N轮的Kappa变化方向                    │
│ 5. 计算提示词diff (本轮 vs 上轮, 本轮 vs Champion)         │
│ 6. 将以上所有数据 + 当前提示词全文 打包发给 Meta-LLM        │
│ 7. Meta-LLM (deepseek-chat) 返回改写后的新提示词          │ 
│ 8. 用新提示词进入 Round N+1                               │
└─────────────────────────────────────────────────────────┘
```

### 1.3 Meta-LLM收到的完整输入

Meta-LLM收到的user message包含以下模块（按优先级排列）：

```
=== CONTEXT ===
Target Role: Annotator（本轮优化目标角色）
Round: 3 / 20

=== OPTIMIZATION HISTORY (全部历史轮次) ===
Round 1: Kappa=0.7340 (Δ=+0.0000) F1=0.8520 R=0.9100 P=0.8300
Round 2: Kappa=0.7420 (Δ=+0.0080) F1=0.8610 R=0.9200 P=0.8350

=== PREVIOUS FAILURES (策略黑名单) ===
Round 2: dropped -0.0120  (DO NOT REPEAT this approach)

=== CHAMPION PROMPT (历史最佳版本 + diff) ===
Best Round 2 (Kappa=0.7540) vs Current Round 3 (Kappa=0.7480, Δ=-0.0060)
[完整diff输出，最多3000字符]

=== WHAT CHANGED LAST ROUND (精确diff) ===
[Round 2 → Round 3 的 unified_diff, 全部 +/- 行]

=== SYSTEM FINAL METRIC TREND ===
Kappa: 0.734 → 0.742 → 0.748
F1:    0.852 → 0.861 → 0.868
Target Kappa: 0.850
Trend: IMPROVING ↗️

=== ROLE METRICS ===
Annotator: Recall=0.920, Precision=0.885, RVS=0.672, Kappa=0.748
Reviewer:  Precision=0.942, Recall=0.900, RVS=0.710, MissedReject=8
Arbitrator: Recall=0.880, Precision=0.910, C_Wrong=3

=== PIPELINE BOTTLENECK ANALYSIS ===
B1 rejection rate: 89/600 dims (pass rate: 85.2%)
A2 actually changed: 42 dimensions (of 89 rejected)
B2 re-rejected: 5 dimensions
C used: 11 times (changed 8)
Final source: A1=558  A2=34  C=8
Pipeline F1: A1_only=0.892 → A1+B1+A2=0.901 → ABABC_full=0.908

=== PER-DIMENSION ERROR PATTERNS (Top 15 cases) ===
Perceived Cause: FN=8, FP=3
  [牙疼得睡不着...] Pred=0 Gold=1 Conf=2.0 | reasoning: ...
Emotional Expression: FP=12 ← 主要瓶颈
  [歯が痛い‼️‼️‼️] Pred=1 Gold=0 Conf=4.5 | reasoning: ...

=== YOUR PREVIOUS OUTPUT (上一轮你生成的提示词, 前1500字符) ===
[prompts_history/Annotator_v2.txt 内容]
Performance change after that round: +0.008 Kappa

=== CURRENT PROMPT (本轮待优化的提示词全文) ===
[~5000字符的Annotator提示词]
```

### 1.4 Meta-LLM的改写策略

Meta-LLM的系统提示词定义了强制三步流程（DIAGNOSE → STRATEGIZE → EXECUTE）：

| 诊断条件 | 策略 |
|---------|------|
| 某维度FP高 (Hallucination) | 收紧 Include criteria + 添加 NEGATIVE EXAMPLE |
| 某维度FN高 (Missed Signal) | 放宽判定条件 + 添加 IMPLICIT SIGNAL example |
| System Recall < 0.92 | 优先优化Annotator：降低置信度门槛 |
| System Precision < 0.92 | 优先优化Reviewer：要求逐字引用原文证据 |
| B1 rejection rate > 20% | Annotator和Reviewer定义不对齐 → 对齐判定标准 |
| A2 rarely changes labels | Annotator无视Revision指令 → 强化Revision Mode提示 |
| C仲裁率 > 5% | Annotator和Reviewer定义矛盾 → 重写争议维度规则 |
| Reviewer误放行(False Pass) | 提高Reviewer审核严格度 |

**硬约束（违反视为失败）：**
- `{lexicon}` 占位符不可触碰（Annotator专用）
- 每维度最多2个示例（新增必须删除旧的——"Prompt Pruning"）
- 修改必须是"改写"而非"追加"
- JSON schema键名(`label`, `confidence`, `reasoning`, `keywords`, `_meta`)不可修改
- 输出必须用 `<H_RAMOS_PROMPT_V1>` 标签包裹
- 提示词长度 > 4500字符被视为失败（"Prompt Bloat"）

### 1.5 早停与回滚机制

```
Round 0a: 零提示词基线（无system prompt → 纯LLM常识）
Round 0b: 单句提示词基线（one-shot最小指令）
Round 0c: 专家v0提示词（优化起点）
  → 产出 00_baseline_comparison.xlsx，证明专家提示词是主要贡献

Round 1..N:
  IF current_kappa > best_kappa + 0.005:
      → 更新 best_kappa, patience_counter=0
      → 存档 Champion Prompt → best_prompts/Annotator_BEST_R{N}.txt
  ELSE:
      → patience_counter += 1

  IF current_kappa < best_kappa - 0.01:
      → 智能回滚: 恢复Champion Prompt, 本轮作废

  IF patience_counter >= 3:
      → 早停: 连续3轮无 > 0.5% 提升

  IF current_kappa >= 0.85:
      → 达标停止

停止后:
  → 自动部署: best_prompts/Annotator_BEST_R{best}.txt
              → best_prompts_final/Annotator_best.txt
              (名字转换, 供05直接加载)
```

### 1.6 审稿人Comment 32的实质

审稿人问："如果3轮达峰，迭代优化真的有效吗？每次API调用独立无记忆，改进可能是巧合。"

**代码层面的真相：** Meta-LLM的"智能"来自三个来源：
1. 人类在系统提示词中预设的决策规则（META_SYSTEM_PROMPT, ~1200字符）
2. 每轮重新计算的完整指标报告（包括10+轮历史、错误案例、维度统计）
3. 结构化输入中的反事实信息（"上次你改了这个 → 效果变差了 → 不要重复"）

它不是在"学习"——它是在"每次拿到更新的诊断报告，尝试不同的改写策略。"
Round 1-3的提升可能是因为：(1) 初始提示词确有改进空间 (2) 指标波动(n=100)的随机性。
3轮后性能退化则通常是因为提示词膨胀导致模型注意力分散（Meta-LLM已被要求遵守pruning规则防此问题）。

---

## 二、混合模型原理（05_三国全量标注 / ababc_utils.py）

### 2.1 架构与分工

三模型分工，所有流水线文件(03/04/05/06)通过 `ababc_utils.py` 共享核心逻辑：

```
deepseek-chat           → Annotator (A)   高召回导向, 首次标注 + 修订
doubao-1-5-pro-32k-250115 → Reviewer (B)   高精确导向, 维度级审核
qwen-max                → Arbitrator (C)  最终裁决, 仅处理争议维度
```

配置一致性（已修复）：所有文件的API Key统一使用 `QWEN_API_KEY`（回退 `DEEPSEEK_API_KEY`），Doubao模型名统一为 `doubao-1-5-pro-32k-250115`。

### 2.2 单帖处理流程（维度级ABABC）

```
帖子: "智齿发炎疼得我布洛芬都压不住，晚上根本睡不着"

┌──────────────────────────────────────────────────────┐
│ A1: deepseek-chat 首次标注全6维度                      │
│   输出: {Perceived Cause: {label:1, conf:4.5,        │
│           keywords:["智齿发炎"]}, ...}               │
├──────────────────────────────────────────────────────┤
│ B1: doubao 逐维度审核                                 │
│   Perceived Cause:     "Approved" ✓                  │
│   Symptom Description: "Approved" ✓                  │
│   Perceived Consequences: "Missed Signal → label=1" ✗│
│   Coping:              "Approved" ✓                  │
│   Emotional Expression:"Approved" ✓                  │
│   Social Interaction:  "Approved" ✓                  │
│   → 1个维度被拒, 5个锁定为A1                         │
├──────────────────────────────────────────────────────┤
│ A2: deepseek-chat 仅修订被拒维度                       │
│   仅修改 Perceived Consequences:                     │
│   {label:1, keywords:["睡不着"],                     │
│    reasoning:"'晚上根本睡不着'. 明确描述了睡眠影响"}   │
│   其他5维保持A1不变                                   │
├──────────────────────────────────────────────────────┤
│ B2: doubao 仅检查A2修改的维度                         │
│   Perceived Consequences: "Approved" ✓               │
│   → 全部通过 → Consensus_R2                          │
├──────────────────────────────────────────────────────┤
│ 最终输出 (由 ababc_utils.build_final_from_dim_trace): │
│   5维度 final_source="A1"                            │
│   1维度 final_source="A2"                            │
│   Exit_Stage="Consensus_R2"                          │
└──────────────────────────────────────────────────────┘
```

如果B2仍然reject → C(qwen-max)介入进行最终仲裁。C只处理争议维度，其他维度保持不变。

### 2.3 _parse_b_verdicts 的判决逻辑

位于 `ababc_utils.py`，是03/04/05/06四个文件的唯一实现（消除了之前的独立副本不一致问题）。

**判决策略：先检查拒绝信号 → 再检查通过信号 → 无法判断时保守拒绝**

```python
# 拒绝信号关键词 (优先级高, 先检查)
REJECT_KEYWORDS = [
    "incorrect", "wrong", "false", "missed", "error", "mistake", "fail",
    "错误", "不正确", "不对", "有误", "不准确", "错了", "漏判",
    "reject", "disapproved", "not approved", "not correct",
    "hallucination", "missing", "invalid", "not found", "should be",
]

# 通过信号关键词 (后检查)
PASS_KEYWORDS = [
    "correct", "pass", "ok", "yes", "good", "fine", "right",
    "正确", "通过", "无误", "可以", "没问题", "对的",
    "approved", "approve", "accept", "valid", "accurate",
]
```

**解析优先级:**
1. 若 `b_output["dimension_feedback"][dim_name]` 存在 → 先匹配拒绝词 → 再匹配通过词
2. 有反馈但无明确信号（无拒绝词也无通过词）→ **保守拒绝**（宁可多走一轮A2也不错放）
3. 无 `dimension_feedback` → 降级使用全局 `is_correct` 字段
4. `b_output` 不是 dict → 全部默认通过

**已知局限：** 只解析通过/不通过，不提取改错方向（→0还是→1）。A2需从反馈文本中自行推断。

### 2.4 缓存机制

每帖的ABABC结果缓存为JSON文件（路径=`annotation_cache_complete/{国家}_{ID}.json`）。重跑时如缓存存在则直接返回，避免重复API调用。缓存包含完整DimTrace，可追溯每个维度的决策链。

### 2.5 中国数据回退逻辑

`load_country_data()` 在主数据文件不存在时自动尝试回退：
```
CHI_combined_data_cleaned.xlsx (主路径)
  → 不存在时回退: aligned_annotations_cleaned.xlsx (备用路径)
  → 都不存在: 返回 None, 跳过该国
```

---

## 三、词典关键词拓展原理（08_多语言SBERT词典扩展）

### 3.1 输入

从05的标注结果Excel中读取每帖每维度的`{dim}_Keywords`列（LLM自由生成的关键词），以及优先使用的`{dim}_StandardConcepts`列。提取去重后得到三国所有唯一关键词。

### 3.2 SBERT嵌入

```
模型: paraphrase-multilingual-MiniLM-L12-v2 (本地部署)
词典锚点(anchors): merged_dictionary_v2.json 中每条规则的每个关键词 → 768维向量
待匹配关键词: LLM生成的自由关键词 → 768维向量
匹配: 余弦相似度(cosine similarity)
```

### 3.3 逐词处理流程

```
关键词: "智齿发炎了"（LLM自由生成）

Step 1 - Top-3检索:
  找到余弦相似度最高的3个词典锚点关键词:
  1. "智齿发炎"     (sim=0.89) → {Perceived Cause, 口腔病理因素, 智齿冠周炎}
  2. "智齿肿痛"     (sim=0.76) → {Perceived Cause, 口腔病理因素, 智齿冠周炎}
  3. "牙龈发炎"     (sim=0.52) → {Perceived Cause, 口腔病理因素, 牙龈炎}

Step 2 - 多锚点投票 (Vote_Strength):
  (Perceived Cause, 口腔病理因素, 智齿冠周炎): 2票
  (Perceived Cause, 口腔病理因素, 牙龈炎): 1票
  → 胜出: {Perceived Cause, 口腔病理因素, 智齿冠周炎}

Step 3 - 跨语言验证 (Cross_Lang_Validated):
  在其他语言的关键词中搜索是否有相同(dim,L1,L2)的高相似度匹配(≥0.60)
  如果有 → effective_score = best_score + 0.05

Step 4 - 阈值判定:
  effective_score ≥ 0.70 → 自动接受, 将新词加入词典
  0.50 ≤ effective_score < 0.70 → 人工审核队列 (分为HIGH/MEDIUM/LOW三档)
  effective_score < 0.50 → 丢弃
```

### 3.4 输出

扩大的词典写回 `merged_dictionary_v2.json`（覆盖更新，旧版备份为 `.backup_round_{N}`）。新增关键词在下一次运行07（共现网络）时可用于标准化匹配。

### 3.5 词典格式兼容

07和08的词典加载均已增加格式探测（修复前仅支持 `{"mapping_rules": [...]}`，格式变化会直接KeyError）：

```python
if "mapping_rules" in data:
    rules = data["mapping_rules"]
elif isinstance(data, list):
    rules = data
else:
    raise ValueError(f"词典格式无法识别。实际 keys: {list(data.keys())[:5]}")
```

### 3.6 循环论证问题

08读取05的输出 → 扩充词典 → 07使用扩充后的词典。但07中优先使用StandardConcepts列（已在05阶段标准化），词典扩充主要影响回退路径（raw keyword lookup）。匹配率的提升部分来自"词典自举"——先往词典里加词，再说匹配率提高了。

---

## 四、全研究运行原理

### 4.1 代码架构

```
ababc_utils.py  ← 共享模块, 03/04/05/06 统一导入
  ├─ get_dim_label / get_dim_confidence / get_dim_reasoning  (维度数据安全提取)
  ├─ parse_b_verdicts  (B审核结论解析, 统一关键词列表)
  ├─ detect_changed_dimensions / build_dim_trace_empty  (维度级轨迹)
  ├─ build_final_from_dim_trace / extract_trace_meta  (结果构建)
  └─ run_ababc_pipeline  (统一ABABC流水线, 供后续迁移使用)

03_排列组合 ← import ababc_utils
04_提示词优化 ← import ababc_utils
05_全量标注 ← import ababc_utils
06_验证集 ← import ababc_utils
```

### 4.2 整体数据流

```
                    ┌──────────────────────────┐
                    │ 测试数据 100条 (人工标注)   │
                    │ 测试数据.xlsx              │
                    └──────────┬───────────────┘
                               │
          ┌────────────────────┼────────────────────┐
          ▼                    ▼                    ▼
   ┌────────────┐      ┌────────────┐      ┌────────────┐
   │02_基线测试  │      │03_排列组合  │      │04_提示词优化│
   │            │      │            │      │            │
   │3提示词×3模型│      │6种ABABC角色 │      │Round 0a/0b  │
   │单模型标注   │      │排列组合     │      │基线测试     │
   │→ Table 1   │      │→ Table 2   │      │→ 迭代优化   │
   │            │      │            │      │→ Champion   │
   │            │      │            │      │→ 自动部署    │
   └────────────┘      └────────────┘      │→ best_       │
                                           │  prompts_    │
                                           │  final/      │
                                           └──────┬───────┘
                                                  │
   ┌────────────┐                                 │
   │01_数据清洗  │                                 │
   │原始Excel   │                                 │
   │→ cleaned   │                                 │
   │  (含回退)   │                                 │
   └─────┬──────┘                                 │
         │                                        │
         ▼                                        ▼
   ┌──────────────────────────────────────────────────┐
   │ 05_全量标注                                       │
   │ 12,082条 × ABABC(deepseek→doubao→qwen)           │
   │ 维度级: A1→B1→A2→B2→C, 每维度溯源                 │
   │ 缓存: annotation_cache_complete/{国家}_{ID}.json  │
   │ 输出: 三国 Detailed_Annotation.xlsx (标签/置信度/   │
   │       推理/关键词/溯源元信息)                       │
   │ 回退: CHI数据自动尝试aligned_annotations.xlsx      │
   └────────────────────┬─────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
   ┌─────────┐   ┌──────────┐   ┌──────────┐
   │06_验证集 │   │08_词典扩展│   │07_共现网络│
   │60条独立  │   │读标注结果 │   │PMI>1.0   │
   │验证泛化  │   │SBERT匹配  │   │共现≥6    │
   │→Fig 4   │   │→扩充词典  │   │freq≥3    │
   └─────────┘   │(覆盖更新) │   │conf≥4    │
                 └─────┬────┘   │→Fig 5-7  │
                       │        │Gephi CSV │
                       └────────┤(格式兼容)│
                                └──────────┘
```

### 4.3 各文件输入→输出对应关系

| 文件 | 输入 | 输出 | 输出用途 |
|------|------|------|---------|
| 01 | 原始Excel(三国) | `*_cleaned.xlsx` | → 05 |
| 02 | 测试数据100条 | `三提示词对比结果.xlsx` | → Table 1 |
| 03 | 测试数据100条 | `ABABC_6Dimensions_Results.xlsx` | → Table 2 |
| 04 | 测试数据100条 | `best_prompts_final/*_best.txt` + `experiment_full_trace/` | → 05, Figure 3 |
| 05 | 01输出 + 04输出 | `final_annotation_results_complete/{三国}/` (3个Excel文件/国) | → 06, 07, 08 |
| 06 | 验证数据60条 + 04输出 | `final_verification_results/` | → Figure 4 |
| 07 | 05输出 | `gephi_nodes_L2_{三国}.csv` + `gephi_edges_L2_PMI_{三国}.csv` | → Figures 5-7 |
| 08 | 05输出 | `merged_dictionary_v2.json` (覆盖更新) | → 07(下次运行) |

### 4.4 执行顺序约束

```
第1步: 02 + 03 (并行, 独立于其他步骤)
         └─ 02: OpenRouter SDK 单次调用, user post 放入 user role (已修复)
         └─ 03: 异步ABABC, 维度级流转

第2步: 04 (依赖: 测试数据)
         └─ 产出 Champion Prompt → auto-deploy至 best_prompts_final/

第3步: 01 (独立) + 05 (依赖: 01输出 + 04输出)
         └─ 01: 文本清洗 + 时间标准化
         └─ 05: 三国全量ABABC, 含缓存续传

第4步: 06 (依赖: 04输出, 独立于05)
         └─ 独立验证集泛化评估

第5步: 08 (依赖: 05输出)
         └─ SBERT词典扩展, 覆盖写回 merged_dictionary_v2.json

第6步: 07 (依赖: 05输出 + 08输出)
         └─ PMI共现网络构建, 输出Gephi CSV
```

### 4.5 API Key 环境变量规范

| 环境变量 | 用途 | 回退 |
|---------|------|------|
| `DEEPSEEK_API_KEY` | DeepSeek (Annotator + Meta-LLM) | — |
| `QWEN_API_KEY` | Aliyun/Qwen (Arbitrator) | `DEEPSEEK_API_KEY` |
| `DOUBAO_API_KEY` | Doubao (Reviewer) | — |
| `DOUBAO_ENDPOINT_ID` | 豆包Endpoint ID | — |
